# scraper/requirements.txt
# Install: pip install -r requirements.txt
# Then:    playwright install chromium

# HTTP & scraping
httpx==0.27.0
playwright==1.44.0
beautifulsoup4==4.12.3
lxml==5.2.2

# Supabase
supabase==2.5.0

# LLM providers (both installed, only one used at runtime)
anthropic==0.28.0
huggingface-hub==0.23.2
requests==2.32.3

# Email — Brevo
sib-api-v3-sdk==7.6.0

# Scheduling & utilities
apscheduler==3.10.4
python-dotenv==1.0.1
pydantic==2.7.1
tenacity==8.3.0          # retry logic
structlog==24.1.0         # structured JSON logging
python-dateutil==2.9.0
hashlib-additional==0.1.0

# Rate limiting
ratelimit==2.2.1
